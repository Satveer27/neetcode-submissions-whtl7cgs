class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        new_arr = []
        for i in nums:
            new_arr.append(i)
        
        for i in nums:
            new_arr.append(i)
        
        return new_arr